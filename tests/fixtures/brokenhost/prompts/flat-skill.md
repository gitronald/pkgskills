---
name: flat-skill
description: A skill body stored as a flat file, outside any skill directory.
---

# flat-skill

Still perfectly loadable by `mli` — the stem names it — but a skills linter
pointed at this package reports the layout, because a skill is meant to be a
directory. That is the whole point of this file.
