---
name: Misfiled Skill
---

# misfiled

Three violations in one file, so a single check run has to report all of them
rather than stopping at the first: the `name` is not the spec's grammar, it
does not match the `misfiled/` directory it sits in, and there is no
`description` at all.
