# A document that names commands the host does not have

Deliberately stale prose, undeclared on `examplehost.HOST` and used only by the
negative test for `assert_prompt_commands`.

Run `{cli} nonexistent` to do the thing, then read `{cli} doc no-such-doc`.

The bare token in prose — {cli} nonexistent — is not a mention, so this line
must not be reported.

BROKEN-BODY-SENTINEL
