---
name: use-solo
description: Use solohost to look things up. Triggers on "look up" or "is this real?".
---

# Use solohost

Look things up with `solohost lookup <id>`. The placeholder {cli} is left alone
here because this host never opted into rendering it.

SOLO-BODY-SENTINEL
