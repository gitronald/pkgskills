---
name: add
description: Add a thing. Triggers on "add a thing" or "new thing".
---

# add

Create a new thing, then validate it (needs a score >= 3):

```bash
{cli} validate .
```

ADD-BODY-SENTINEL
