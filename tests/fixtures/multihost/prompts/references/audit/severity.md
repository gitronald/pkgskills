---
name: severity
description: How an audit ranks what it found.
---

# Severity

A second document, with frontmatter, so the split is exercised on the doc path
too: `doc` strips it the way `skill` does.

AUDIT-SEVERITY-SENTINEL
