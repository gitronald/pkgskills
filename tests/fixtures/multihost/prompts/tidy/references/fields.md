# Fields a tidy pass may rewrite

A reference document: loaded by a step, not by a trigger. It is namespaced
under the skill that owns it, and it uses `{cli}` like any other body — run
`{cli} install --check` once the rewrite is done.

TIDY-FIELDS-SENTINEL
