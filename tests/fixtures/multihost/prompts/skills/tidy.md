---
name: tidy
description: Tidy a workspace with multihost. Triggers on "tidy up" or "clean this".
---

# tidy

Read `{cli} doc tidy/fields` before rewriting anything.

Tidy the workspace, then confirm the result with `{cli} install --check`.

TIDY-BODY-SENTINEL
